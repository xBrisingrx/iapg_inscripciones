<section class="mx-auto g-pa-20">
	<%= render('header') %>
	<div class="row justify-content-center">
		<% if @limit %>
			<h1> Se completo el cupo de inscripciones. </h1>
		<% else %>
			<div class="col-12 col-sm-12 col-md-10">
				<%= render 'form', inscription: @inscription %>
			</div>
		<% end %>
	</div>
</section>

